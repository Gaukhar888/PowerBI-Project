-- Задание 1: Найти пользователей, купивших 2 корма для животных (без Kitekat) с 1 по 15 августа
SELECT DISTINCT o.customer_id
FROM orders o
JOIN order_lines ol ON o.order_id = ol.order_id
JOIN products p ON ol.product_id = p.product_id
WHERE o.order_date BETWEEN '2024-08-01' AND '2024-08-15'
AND p.category = 'Корм для животных'
AND p.product_name != 'Корм Kitekat для кошек, с кроликом в соусе, 85 г'
GROUP BY o.customer_id
HAVING COUNT(DISTINCT ol.product_id) >= 2;

-- Задание 2: Найти топ-5 популярных товаров в заказах СПб (15-30 августа)
SELECT p.product_name, COUNT(ol.product_id) AS order_count
FROM orders o
JOIN order_lines ol ON o.order_id = ol.order_id
JOIN products p ON ol.product_id = p.product_id
WHERE o.order_date BETWEEN '2024-08-15' AND '2024-08-30'
AND o.city = 'Санкт-Петербург'
GROUP BY p.product_name
ORDER BY order_count DESC
LIMIT 5;